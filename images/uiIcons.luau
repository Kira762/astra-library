--[[
Single source of truth for Astra's window and Settings UI icons.
Names are intentionally lowercase and case-sensitive. Custom assets resolve
from the executor's custom_asset/ directory.
]]
return {
    window = "sirius",
    close = "close",
    minimise = "minimise",
    maximise = "maximise",
    settings = "settings",
    search = "search",
    chevron = "chevron",
    check = "check",
    config = "config",
}
